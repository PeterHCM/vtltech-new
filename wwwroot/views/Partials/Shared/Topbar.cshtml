@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<IPublishedContent>
@using Umbraco.Cms.Core.Models
@using Umbraco.Extensions
@using System.Globalization

@{
    var settings = Umbraco.ContentAtRoot().FirstOrDefault(x => x.ContentType.Alias == "companySettings");
    if (settings == null)
    {
        return;
    }
    var address = settings.Value<string>("companyAddress") ?? "19A Cộng Hòa, Phường Bảy Hiền, Thành phố Hồ Chí Minh";
    var locationUrl = settings.Value<IEnumerable<Link>>("companyLocation")?.FirstOrDefault()?.Url ?? "#";
    var facebook = settings.Value<IEnumerable<Link>>("facebookUrl")?.FirstOrDefault()?.Url ?? "#";
    var youtube = settings.Value<IEnumerable<Link>>("youtubeUrl")?.FirstOrDefault()?.Url ?? "#";
    var twitter = settings.Value<IEnumerable<Link>>("twitterUrl")?.FirstOrDefault()?.Url ?? "#";

    var currentCulture = CultureInfo.CurrentCulture.Name;
    var isVietnamese = currentCulture == "vi-VN";
    var switchLangUrl = isVietnamese ? Model.Url("en") : Model.Url("vi-VN");
    var switchLangLabel = isVietnamese ? "EN" : "VI";
    var switchLangTitle = isVietnamese ? "English" : "Tiếng Việt";
    var currentDomain = Context.Request.Host.Value;
    var isIntranet = currentDomain.Contains("intranet");
}
<div class="bg-gray-200 text-gray-800 dark:bg-gray-900 dark:text-white text-sm py-1 px-4 md:px-6">
    <div class="max-w-7xl mx-auto flex justify-between items-center">
        
        @if (!string.IsNullOrWhiteSpace(address) && locationUrl != "#")
        {
            <div class="hidden md:flex items-center gap-2 text-xs">
                <div class="flex items-center gap-1 whitespace-nowrap">
                    <a href="@locationUrl" target="_blank" class="flex items-center gap-1 hover:text-primary dark:text-gray-200"
                       title="@(isVietnamese ? "Xem trên bản đồ" : "View on map")" aria-label="Địa chỉ công ty">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-primary shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M17.657 16.657L13 19.914l-4.657-3.257A2 2 0 018 14.586V12h8v2.586a2 2 0 01-.343 1.071z M12 11a3 3 0 100-6 3 3 0 000 6z" />
                        </svg>
                        @address
                    </a>
                </div>
            </div>
        }

        <div class="flex items-center gap-3 ml-auto w-full md:w-auto justify-end">
            
            @* 💥 Tối ưu 3: Thêm kiểm tra "==" "#" để không hiện icon nếu URL chỉ là placeholder *@
            @if (facebook != "#")
            {
                <a href="@facebook" target="_blank" aria-label="Facebook" title="Facebook"
                   class="w-7 h-7 p-1 flex items-center justify-center rounded-full bg-gray-200 dark:bg-gray-700 hover:opacity-80 transition shrink-0">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" class="w-4 h-4">
                        <circle cx="16" cy="16" r="16" fill="#1877F2"/>
                        <path d="M21.5 11h-2.4c-.3 0-.6.3-.6.6v1.9h3l-.4 3h-2.6v7h-3v-7h-2v-3h2v-2.2c0-2 1.2-3.1 3-3.1h2.6v3z" fill="#fff"/>
                    </svg>
                </a>
            }
            @if (youtube != "#")
            {
                <a href="@youtube" target="_blank" aria-label="YouTube" title="Youtube"
                   class="w-7 h-7 p-1 flex items-center justify-center rounded-full bg-gray-200 dark:bg-gray-700 hover:opacity-80 transition shrink-0">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" class="w-4 h-4">
                        <rect width="32" height="32" rx="6" fill="#FF0000"/>
                        <polygon points="13,10 23,16 13,22" fill="#fff"/>
                    </svg>
                </a>
            }
            <div class="@(isIntranet ? "hidden" : "") rounded">
                <a href="@switchLangUrl"
                title="@switchLangTitle"
                aria-label="@(isVietnamese ? "Chuyển sang tiếng Anh" : "Switch to Vietnamese")"
                class="flex items-center gap-1 text-xs hover:text-primary transition-colors shrink-0">      
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M12 2a10 10 0 100 20 10 10 0 000-20zM2 12h20M12 2c2.5 3 2.5 17 0 20M12 2c-2.5 3-2.5 17 0 20" />
                    </svg>
                    <span class="uppercase font-bold text-sm">@switchLangLabel</span>
                </a>
            </div>
            <button id="theme-toggle" aria-label="Chuyển đổi chế độ sáng/tối" class="flex items-center gap-1 text-xs hover:text-primary transition-colors shrink-0">
                <svg id="theme-toggle-dark-icon" class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>
                </svg>
                <svg id="theme-toggle-light-icon" class="w-4 h-4 hidden" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M10 2a8 8 0 110 16 8 8 0 010-16zM3 10a7 7 0 007 7V3a7 7 0 00-7 7z"></path>
                </svg>
            </button>
        </div>
    </div>
</div>